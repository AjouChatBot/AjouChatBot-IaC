apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amate.fullname" . }}-test-connection"
  labels:
    {{- include "amate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
