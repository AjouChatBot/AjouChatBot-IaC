apiVersion: v1
kind: Service
metadata:
  name: {{ include "amate.fullname" . }}
  labels:
    {{- include "amate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amate.selectorLabels" . | nindent 4 }}
